import 'package:flutter/material.dart';

class TaxiAcceptedOrdersPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Страница принятых заказов'),
    );
  }
}
