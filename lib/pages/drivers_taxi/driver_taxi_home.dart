import 'package:flutter/material.dart';

class DriverTaxiHome extends StatelessWidget {
  const DriverTaxiHome({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}