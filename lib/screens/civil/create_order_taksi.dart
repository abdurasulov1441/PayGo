import 'package:flutter/material.dart';

class CreateOrderTaksi extends StatelessWidget {
  const CreateOrderTaksi({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
