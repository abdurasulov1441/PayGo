import 'package:flutter/widgets.dart';

class TruckDriversPage extends StatelessWidget {
  const TruckDriversPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
